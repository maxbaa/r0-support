# rθ — Privacy & Support

Static GitHub Pages site containing the official privacy policy and support information for rθ.

## GitHub Pages

Publish from the repository's `main` branch and the root directory. The resulting paths are:

- `/privacy/`
- `/support/`
